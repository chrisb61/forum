{
  "name": "@forum/database",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "seed": "ts-node --require dotenv/config --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts dotenv_config_path=../../apps/api/.env",
    "studio": "prisma studio"
  },
  "prisma": {
    "seed": "ts-node --require dotenv/config --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts dotenv_config_path=../../apps/api/.env"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "argon2": "^0.31.2"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "prisma": "^5.7.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
